import React from 'react';

export default function PageWrapper({cloudy, children}}) {
    return(
        <div style={{
            backgroundColor=
        }}>
            {children}
        </div>
    )
}